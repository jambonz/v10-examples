# Webhook Examples

Examples using HTTP webhook transport instead of WebSocket. In webhook mode, jambonz sends an HTTP POST to your server for each call event, and your server responds with a JSON array of verbs.
