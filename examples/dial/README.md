# dial

Examples for making outbound calls and transfers using the `dial` verb.

| Example | Description |
|---------|-------------|
| [outbound](./outbound) | Making outbound calls |
| [livevox-passthrough](./livevox-passthrough) | Bridge an inbound call to a LiveVox trunk, forwarding `X-LiveVox`/`X-Prodigal` SIP headers |
